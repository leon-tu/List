#ifndef __QUICK_SORT__
#define __QUICK_SORT__

#include "com_type.h"

INT ArrayAdjust(INT *pArray, UINT i, UINT j);		// smallest to biggest, i is in left, j is in rigth
void QuickSortFun(INT *pArray, UINT uiL, UINT uiR);

#endif
